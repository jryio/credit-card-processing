var expect = require('chai').expect
var commands = require('../lib/commands.js')

describe('Commands', function () {
  describe('.add()', function () {
    it('should add a new credit card to dataStore', function () {

    })
  })
})
